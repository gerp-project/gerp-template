name: GERP Synchronize Templates

on:
  push:
    branches:
      - main
  workflow_dispatch:
  
jobs:
  sync-templates:
    name: Sync Templates
    uses: gerp-project/gerp/.github/workflows/sync_templates.yaml@main
    with:
      committer: David Gamero <david340804@gmail.com>
    secrets:
      PAT: ${{ secrets.PAT }}
      GPG_PRIVATE_KEY: ${{ secrets.GPG_PRIVATE_KEY }}
      GPG_PASSPHRASE: ${{ secrets.GPG_PASSPHRASE }}
